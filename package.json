{
  "name": "test-extension",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "chrome": "^0.1.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "typescript": "^5.9.2"
  }
}
