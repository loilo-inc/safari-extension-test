{
  "name": "test-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "ts-node -P tsconfig.server.json server/server.ts",
    "build:server": "tsc -p tsconfig.server.json",
    "start:server": "node dist/server/server.js"
  },
  "dependencies": {
    "express": "^5.2.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/cors": "^2.8.17",
    "@types/chrome": "^0.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
